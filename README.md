# Neural Operators for Continuous State-Space Modeling

# Setup
## Clone Repo
```bash
git clone [URL]
```
## Install Dependencies
```bash
cd opssm
conda env create -n opssm -f environment.yml #choose correct .yml depending on system
conda activate opssm
```

